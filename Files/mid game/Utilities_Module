def move_n_dir(n, dir):
	for i in range(n):
		move(dir)
		
		
def is_even(n):
	return n % 2 == 0	
	
def should_tree(posx,posy):
	if not posy == 0:
		if is_even(posx + posy) or posx == posy:
			return True
	return False
